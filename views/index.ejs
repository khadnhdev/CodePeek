<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Code Renderer</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/prism/1.24.1/themes/prism.min.css" rel="stylesheet" />
</head>
<body>
    <div class="container mt-5">
        <h1>Code Renderer</h1>

        <!-- API Documentation -->
        <div class="card mb-4">
            <div class="card-header d-flex justify-content-between align-items-center">
                <h5 class="mb-0">API Documentation</h5>
                <button class="btn btn-sm btn-outline-secondary" onclick="copyApiExample()">
                    <i class="bi bi-clipboard"></i> Copy Example
                </button>
            </div>
            <div class="card-body">
                <h6>Endpoint:</h6>
                <pre><code class="language-http">POST /api/render</code></pre>

                <h6>Request:</h6>
                <pre><code class="language-json">{
    "content": "Your HTML, CSS, JS or Mermaid code here"
}</code></pre>

                <h6>Response:</h6>
                <pre><code class="language-json">{
    "url": "http://your-domain.com/view/generated-id",
    "id": "generated-id"
}</code></pre>

                <h6>Example using curl:</h6>
                <pre><code class="language-bash">curl -X POST <%= baseUrl %>/api/render \
    -H "Content-Type: application/json" \
    -d '{"content": "<!DOCTYPE html><html><body><h1>Hello World</h1></body></html>"}'</code></pre>

                <h6>Example using JavaScript:</h6>
                <pre><code class="language-javascript">const response = await fetch('<%= baseUrl %>/api/render', {
    method: 'POST',
    headers: {
        'Content-Type': 'application/json',
    },
    body: JSON.stringify({
        content: '<!DOCTYPE html><html><body><h1>Hello World</h1></body></html>'
    })
});
const data = await response.json();
console.log(data.url); // URL to view the rendered content</code></pre>
            </div>
        </div>

        <!-- Original content -->
        <div class="row">
            <div class="col-md-6">
                <div class="mb-3">
                    <textarea class="form-control" id="codeInput" rows="15" 
                        placeholder="Nhập code HTML, CSS, JavaScript hoặc Mermaid...

Ví dụ HTML/CSS/JS:
<!DOCTYPE html>
<html>
<head>
    <style>
        /* CSS ở đây */
    </style>
</head>
<body>
    <!-- HTML ở đây -->
    <script>
        // JavaScript ở đây
    </script>
</body>
</html>

Hoặc Mermaid:
graph TD
    A[Client] -->|HTTP Request| B[Load Balancer]
    B --> C[Server1]
    B --> D[Server2]"></textarea>
                </div>
                <button class="btn btn-primary" onclick="renderCode()">Render</button>
            </div>
            <div class="col-md-6">
                <div id="output" class="border p-3 min-vh-50"></div>
            </div>
        </div>
    </div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/prism/1.24.1/prism.min.js"></script>
    <script>
        // Copy API example function
        function copyApiExample() {
            const example = {
                method: 'POST',
                url: '<%= baseUrl %>/api/render',
                headers: {
                    'Content-Type': 'application/json'
                },
                body: {
                    content: '<!DOCTYPE html><html><body><h1>Hello World</h1></body></html>'
                }
            };
            
            navigator.clipboard.writeText(JSON.stringify(example, null, 2))
                .then(() => {
                    const btn = document.querySelector('.card-header button');
                    btn.innerHTML = '<i class="bi bi-check"></i> Copied!';
                    setTimeout(() => {
                        btn.innerHTML = '<i class="bi bi-clipboard"></i> Copy Example';
                    }, 2000);
                });
        }

        // Original render function
        async function renderCode() {
            const code = document.getElementById('codeInput').value;
            const output = document.getElementById('output');

            // Lưu vào database
            const response = await fetch('/render', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                },
                body: JSON.stringify({ code })
            });
            const data = await response.json();

            // Clear previous output
            output.innerHTML = '';

            // Create iframe for output
            const iframe = document.createElement('iframe');
            iframe.style.width = '100%';
            iframe.style.height = '400px';
            iframe.style.border = 'none';
            output.appendChild(iframe);

            // Load content in iframe
            iframe.src = `/render-content/${data.id}`;

            // Hiển thị link để xem lại
            const viewLink = document.createElement('div');
            viewLink.innerHTML = `<a href="/view/${data.id}" class="mt-3 d-block">Permanent link</a>`;
            output.appendChild(viewLink);
        }
    </script>
</body>
</html> 